# Example environment variables for local dev and preview deploys.
# Copy this to `.env` for local development, or let CI use it for previews.

# Required
COOKIE_SECRET=PREVIEW_ONLY_COOKIE_SECRET_32_CHARS_MINIMUM____

# Optional (preview uses request origin by default)
APP_BASE_URL=

# Resend (optional; preview deploys overwrite these to point at the mock services)
RESEND_API_BASE_URL=
RESEND_API_KEY=
# From address used for password reset emails (sent via Resend).
# Intentionally `reset@...` (it does not need to match the "Resend" name).
RESEND_FROM_EMAIL=reset@epic-agent.dev

# Optional feature toggles / admin controls
# Optional GitHub token for GitHub API access; use a long-lived PAT (not the Actions token).
GITHUB_TOKEN=
WORKSHOP_INDEX_ADMIN_TOKEN=
# By default, the manual reindex endpoint only works on localhost to prevent
# production deployments from calling GitHub. Set to 1/true/yes to allow remote.
WORKSHOP_INDEX_ALLOW_REMOTE_REINDEX=
WORKSHOP_CONTEXT_DEFAULT_MAX_CHARS=
WORKSHOP_CONTEXT_HARD_MAX_CHARS=
